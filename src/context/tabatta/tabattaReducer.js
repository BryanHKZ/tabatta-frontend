import {
  CREAR_TABATTA,
  CREAR_TABATTA_ERROR,
  CREAR_TABATTA_EXITO,
} from "../../types";

export const tabattaReducer = (state, action) => {
  switch(action.type) {
    default:
      return state;
  }
}
import {
  AGREGAR_EJERCICIO, AGREGAR_EJERCICIO_ERROR, AGREGAR_EJERCICIO_EXITO
} from "../../types";

export const exerciseReducer = (state, action) => {
  switch(action.type) {
    default:
      return state;
  }
}